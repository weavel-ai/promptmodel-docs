---
description: 'prompt init - Promptmodel CLI Documentation'
---

# init

```bash
prompt init
```

> Generates a `promptmodel_dev.py` file in the current directory.

This file is used as an entry point for connecting your codebase with the dashboard.

All `PromptModel`/`ChatModel` code should be imported to this file via `DevClient`.
